## 1. SKILL 结构与生成基础

- [x] 1.1 创建 `.agents/skills/metastar-health/` 目录结构，添加 `SKILL.md`、`references/` 资源文档和生成区域标记
- [x] 1.2 实现 API 能力元数据与导出扫描逻辑，基于 `api/index.ts` 识别可用业务模块
- [ ] 1.3 实现 SKILL 生成/校验脚本，支持更新 `description` 与 `references/api-map.md`，并支持 `--check` 模式

## 2. 分层资源文档

- [ ] 2.1 编写入口 `SKILL.md`，包含触发场景、认证提醒、按需资源路由和避免真实副作用说明
- [ ] 2.2 编写同步查询资源文档，覆盖论文搜索、实体补全、药物搜索、HPA 画像和 GBD 查询函数
- [ ] 2.3 编写文档处理资源文档，覆盖 OCR、临床前文本提取和文献解析的提交、查询与轮询函数
- [ ] 2.4 编写靶点工作流资源文档，覆盖靶点助手和靶点快速评估的输入约束、状态流转与结果处理

## 3. 验证与集成

- [ ] 3.1 添加生成逻辑测试，覆盖导出扫描、description 生成、能力表生成和校验失败路径
- [ ] 3.2 添加 SKILL 结构校验，确保入口精简、资源文档存在、无真实凭据且不默认访问真实网络
- [ ] 3.3 将生成和校验命令接入项目脚本，并运行 `pnpm check` 与相关测试确认通过
