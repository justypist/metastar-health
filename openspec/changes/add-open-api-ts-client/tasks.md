## 1. 公共客户端基础

- [x] 1.1 建立 `api/` TypeScript 模块结构，新增统一导出入口、配置读取和默认 Base URL。
- [x] 1.2 定义公共响应、错误、客户端选项、任务状态、实体和文件输入类型，确保不使用 `any`。
- [x] 1.3 实现公共请求封装，支持认证头、查询参数、JSON 请求体、响应解析和服务端错误抛出。
- [x] 1.4 实现文件上传辅助与通用异步任务轮询辅助，支持超时、间隔、完成和失败状态处理。

## 2. 异步文件与文本处理 API

- [x] 2.1 实现 OCR API 函数：提交 OCR 任务、查询 OCR 结果、轮询 OCR 结果。
- [x] 2.2 实现临床前文本提取 API 函数：提交文本提取任务、查询文本提取结果、轮询文本提取结果。
- [x] 2.3 实现文献解析 API 函数：提交文献解析任务、查询文献解析结果、轮询文献解析结果。

## 3. 同步搜索与查询 API

- [x] 3.1 实现论文搜索与论文健康检查 API 函数。
- [x] 3.2 实现实体自动补全 API 函数。
- [x] 3.3 实现药物搜索 API 函数，包含结构化实体参数和表格化响应类型。
- [x] 3.4 实现 HPA 靶点画像查询 API 函数。
- [x] 3.5 实现 GBD 疾病负担数据查询 API 函数。

## 4. 靶点任务 API

- [x] 4.1 实现靶点助手 API 函数：提交任务、查询结果、轮询结果，并处理 `success=false` 的验证未通过响应。
- [x] 4.2 实现靶点快速评估 API 函数：提交批量任务、查询结果、轮询结果。

## 5. 验证与文档化

- [ ] 5.1 补充最小使用示例或注释，说明如何配置 `.env` 并从 `api/index.ts` 调用函数。
- [ ] 5.2 运行可用的 TypeScript/Node 检查，确认导出、类型和语法通过。
